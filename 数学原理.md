下面把完整超分方案中的每一个改进拆开，说明它解决什么问题、数学依据是什么，以及它能严格保证什么。所有方案都建立在你给出的 exact local↔global 相机变换上。

---

# 1. 统一数学框架

在某个生成阶段 (R)，例如 C64、Shape512 或 Shape1024，定义三类空间。

Global token 空间：

[
V_g^{(R)}=\mathbb R^{N_g\times32}
]

第 (i) 个 global token 对应 global 规范空间中的一个 cell：

[
G_i^{(R)}\subset[-1,1]^3
]

第 (k) 个 tile 的 local token 空间：

[
V_{l,k}^{(R)}=\mathbb R^{N_{l,k}\times32}
]

第 (j) 个 local token 对应 local cell：

[
L_{k,j}^{(R)}
]

经过 exact 变换：

[
T_k:L_k\rightarrow G
]

映射回 global 空间后：

[
\widetilde L_{k,j}^{(R)}
========================

T_k\left(L_{k,j}^{(R)}\right)
]

Global cell 和所有 mapped local cell 的共同细分形成公共空间原子：

[
\mathcal A^{(R)}={A_r}_{r=1}^{N_H}
]

公共高分辨率 latent 空间为：

[
V_H^{(R)}=\mathbb R^{N_H\times32}
]

后面所有噪声、低频、高频和多 tile 融合，都是在这个统一空间中定义的。

---

# 2. 改进一：使用 exact 相机变换，而不是固定缩放和平移

## 2.1 原问题

过去容易把 local→global 写成：

[
x_g=\frac14x_l+\Delta x,\qquad
y_g=\frac14y_l+\Delta y
]

但你的精确变换是：

[
x_g=a_x(z)x_l+b_x(z)
]

[
y_g=a_y(z)y_l+b_y(z)
]

[
z_g=z_l=z
]

其中：

[
a_x(z)=
\frac{s_gD_g(z)f_{l,x}}
{s_lD_l(z)r_xf_{g,x}}
]

[
a_y(z)=
\frac{s_gD_g(z)f_{l,y}}
{s_lD_l(z)r_yf_{g,y}}
]

因此缩放和平移都与深度有关。

## 2.2 数学依据

透视投影是：

[
u=f\frac{X}{D}+c
]

同一个像素偏移反投影到三维后的横向位移与深度成正比：

[
X=\frac{(u-c)D}{f}
]

所以 crop 光心偏移在三维空间中不可能是固定平移，而必须乘上：

[
D_g(z),\qquad D_l(z)
]

这也是为什么 local→global 一般不是单一 4×4 affine matrix。

## 2.3 它保证什么

使用 exact (T_k) 后可以严格保证：

[
T_k^{-1}(T_k(q_l))\approx q_l
]

[
T_k(T_k^{-1}(q_g))\approx q_g
]

你的数值要求是：

[
\max|\Delta q|<2\times10^{-5}
]

这保证后面的误差来自生成和融合，而不是空间错位。

---

# 3. 改进二：将 token 看成有体积的 cell，而不是点

## 3.1 原问题

只变换 token center 会丢失两类信息：

1. token 在 global 空间中实际有多大；
2. token 横跨了哪些 global 邻接 cell。

两个中心位置相同、分辨率不同的 token，如果只看中心，会被认为完全相同，这是错误的。

## 3.2 数学依据

生成模型中的一个 sparse token 实际代表某个有限空间区域上的 latent。

C64 lattice 节点为：

[
q_n=-1+\frac{2n}{63}
]

应给每个节点定义 Voronoi cell，而不是零体积点。

映射后 local cell 的体积是：

[
V_{k,j}^l
=========

\int_{L_{k,j}}
\left|\det J_{T_k}(q)\right|dq
]

你的变换 Jacobian 是上三角结构：

[
J_{T_k}
=======

\begin{bmatrix}
a_x(z)&0&\partial_zx_g\
0&a_y(z)&\partial_zy_g\
0&0&1
\end{bmatrix}
]

所以：

[
\boxed{
\det J_{T_k}=a_x(z)a_y(z)
}
]

当前配置下大致为：

[
\det J_{T_k}\approx\frac1{16}
]

即 local token 在 global XY 上约缩小四倍、体积约缩小十六倍，但 z 尺度没有缩小。

## 3.3 为什么需要公共原子

Global cell 与 local mapped cell 通常并不严格嵌套。

公共原子是它们的共同 refinement：

[
A_r\subset G_i
]

并且对于某个 tile：

[
A_r\subset\widetilde L_{k,j}
]

或者：

[
A_r\cap\widetilde L_{k,j}=\varnothing
]

这样每一个原子都具有唯一、无歧义的物理归属。

---

# 4. 改进三：噪声使用同一个空间白噪声 realization

## 4.1 原问题

分别初始化：

[
\epsilon_g\sim\mathcal N(0,I)
]

[
\epsilon_l\sim\mathcal N(0,I)
]

虽然 global 和 local 的边缘分布都正确，但它们是两个完全无关的随机轨迹。

即使条件图像和空间位置对应，flow 一开始就可能走向不同生成模态。

## 4.2 数学构造

在 global 规范空间定义 32 通道 Gaussian white-noise measure：

[
W=(W_1,\ldots,W_{32})
]

对于任意区域 (D)，定义归一化区域噪声：

[
\boxed{
\epsilon(D)
===========

\frac1{\sqrt{|D|}}\int_DdW
}
]

Global token：

[
\epsilon_i^g=\epsilon(G_i)
]

Local token：

[
\epsilon_{k,j}^l
================

\epsilon(\widetilde L_{k,j})
]

## 4.3 为什么除以体积平方根

白噪声积分满足：

[
\operatorname{Var}\left(\int_DdW\right)=|D|
]

因此：

[
\operatorname{Var}\left(
\frac1{\sqrt{|D|}}\int_DdW
\right)=1
]

所以每个 token 不论体积大小，都仍然满足训练时的标准高斯边缘分布：

[
\epsilon(D)\sim\mathcal N(0,I_{32})
]

## 4.4 Global/local 相关性

两个 token 区域 (D_1,D_2) 的协方差为：

[
\boxed{
\operatorname{Cov}
\left(
\epsilon(D_1),\epsilon(D_2)
\right)
=======

\frac{|D_1\cap D_2|}
{\sqrt{|D_1||D_2|}}
}
]

因此：

[
\operatorname{Cov}
(\epsilon_{k,j}^l,\epsilon_i^g)
===============================

\frac{
|\widetilde L_{k,j}\cap G_i|
}{
\sqrt{
|\widetilde L_{k,j}||G_i|
}
}
]

它自然处理：

* 完全相同区域：相关系数为 1；
* 部分重叠：相关系数在 0 和 1 之间；
* 完全不重叠：相关系数为 0；
* 同中心但不同尺度：只共享重叠部分；
* 到某个 global token 距离相同但方向不同：由于实际区域不同，噪声不同。

## 4.5 为什么这个协方差一定合法

定义归一化区域指示函数：

[
\phi_D(x)=\frac{\mathbf1_D(x)}{\sqrt{|D|}}
]

则：

[
K(D_1,D_2)
==========

\frac{|D_1\cap D_2|}
{\sqrt{|D_1||D_2|}}
===================

\langle\phi_{D_1},\phi_{D_2}\rangle_{L^2}
]

因此 (K) 是一个 Gram matrix，必然半正定。

这避免了手工设计 cross-covariance 时可能出现：

[
\begin{bmatrix}
I&C^\top\
C&I
\end{bmatrix}
\nsucceq0
]

的问题。

## 4.6 公共原子的离散实现

给每个原子采样：

[
\xi_r\sim\mathcal N(0,I_{32})
]

则：

[
\epsilon_i^g
============

\frac{
\sum_{A_r\subset G_i}
\sqrt{|A_r|}\xi_r
}{
\sqrt{|G_i|}
}
]

[
\epsilon_{k,j}^l
================

\frac{
\sum_{A_r\subset\widetilde L_{k,j}}
\sqrt{|A_r|}\xi_r
}{
\sqrt{|\widetilde L_{k,j}|}
}
]

这相当于对白噪声积分的有限体积离散。

---

# 5. 改进四：噪声和 latent/velocity 使用不同归一化

虽然它们使用相同交叠体积：

[
\Omega_{kji}
============

|\widetilde L_{k,j}\cap G_i|
]

但权重不同。

## 噪声

[
C_{kji}^{noise}
===============

\frac{\Omega_{kji}}
{\sqrt{V_{k,j}^lV_i^g}}
]

这是 L2 归一化，目标是保持方差。

## 普通 latent 场

Global field 投到 local：

[
(P_{l\leftarrow g}x_g)_j
========================

\frac{
\sum_i\Omega_{kji}x_i^g
}{
V_{k,j}^l
}
]

这是 L1/质量归一化，目标是保持常量场。

如果：

[
x_i^g=x_0,\quad\forall i
]

则：

[
(Px_g)_j=x_0
]

如果误用噪声的平方根权重，常量场通常不会保持，latent 幅值会随 token 数量和分辨率变化。

---

# 6. 改进五：Global anchor 与 local candidate 分开

## 6.1 原问题

只使用 projected global support：

* topology 稳定；
* 但没有真正超分出来的新几何。

只使用 local native support：

* 可以生成新结构；
* 但不同 tile 的 topology 容易不一致；
* 与 global 基线的 support Jaccard 很低；
* 容易出现浮空点、双壳和局部幻觉。

## 6.2 数学上为什么要分开

Topology 是离散变量：

[
z_r\in{0,1}
]

而 latent flow 状态是连续变量：

[
x_r(t)\in\mathbb R^{32}
]

普通 ODE：

[
\frac{dx}{dt}=v(x,t)
]

要求状态空间维数在积分过程中固定。

如果 token support 在 ODE 中不断增删，实际上状态空间本身在变：

[
\mathbb R^{N(t)\times32}
]

这不再是普通固定维 ODE。

因此需要将问题拆成：

1. 离散 topology 选择；
2. 固定 support 上的连续 flow。

## 6.3 Anchor 的数学作用

Global anchor 是硬约束：

[
z_r=1,\qquad r\in S_g^{anchor}
]

它保证最终 high-resolution support 至少包含 global baseline 的主体拓扑。

## 6.4 Candidate 的数学作用

Local native support 是假设集合：

[
z_r\in{0,1},\qquad r\in S^{candidate}
]

可以用一个离散能量表示：

[
E(z)
====

\sum_r\psi_r(z_r)
+
\lambda\sum_{(r,s)}\psi_{rs}(z_r,z_s)
]

其中：

* (\psi_r)：来自 tile 数量、可见性、深度和置信度的 unary evidence；
* (\psi_{rs})：鼓励局部连通、抑制孤立浮空点的 pairwise term。

最终选择：

[
z^*=\arg\min_zE(z)
]

工程上不一定真的跑图割，但 anchor/candidate/连通性规则来自这个离散优化原理。

---

# 7. 改进六：同步 clean endpoint，而不是 raw velocity

## 7.1 为什么 raw velocity 不能直接平均

Global 和 local 预测分别是：

[
v_g=F(x_g,c_g,t)
]

[
v_l=F(x_l,c_l,t)
]

它们的：

* 当前状态 (x_g,x_l) 不同；
* 图像条件 (c_g,c_l) 不同；
* support 不同；
* 相机不同。

即使某些 token 对应同一物理位置，两个 velocity 也是在不同状态点上评价的向量场：

[
v_g(x_g,t)\neq v_l(x_l,t)
]

直接平均相当于把不同切空间上的向量相加。

## 7.2 Clean endpoint 是共同目标变量

设概率路径：

[
x_t=\alpha(t)x_0+\beta(t)x_1
]

速度为：

[
v_t=\dot\alpha(t)x_0+\dot\beta(t)x_1
]

解这个二元线性方程，可得到预测终点：

[
\boxed{
\hat x_1
========

\frac{
-\dot\alpha(t)x_t+\alpha(t)v_t
}{
\alpha(t)\dot\beta(t)-\dot\alpha(t)\beta(t)
}
}
]

在线性 rectified-flow 路径下：

[
x_t=(1-t)x_0+tx_1
]

因此：

[
\boxed{
\hat x_1=x_t+(1-t)v_t
}
]

无论 global 还是 local，(\hat x_1) 表示的都是“模型认为最终 clean latent 应该是什么”，所以比 raw velocity 更适合映射到统一空间比较。

## 7.3 同步后再恢复 velocity

得到统一 endpoint：

[
\hat x_1^{sync}
]

后，再根据当前路径恢复同步速度。

在线性路径下：

[
\boxed{
v_t^{sync}
==========

\frac{\hat x_1^{sync}-x_t}{1-t}
}
]

所以不是放弃 flow，而是把同步放在更可比较的 endpoint 空间中进行。

---

# 8. 改进七：用有限体积投影定义高频和低频

这是整个方案最核心的数学部分。

## 8.1 Global C64 能表示的子空间

在公共原子空间中，global token 只能在整个 global cell 内表达一个常量。

定义 global→atom 广播算子：

[
(P_gx_g)*r=x*{g,i(r)}
]

矩阵形式：

[
P_g\in\mathbb R^{N_H\times N_g}
]

每行只有一个 1，指向该原子的 global parent。

## 8.2 质量矩阵

定义原子体积质量矩阵：

[
M_H=\operatorname{diag}
(|A_1|,\ldots,|A_{N_H}|)
]

公共空间中的自然内积是：

[
\langle x,y\rangle_{M_H}
========================

x^\top M_Hy
]

它等价于连续场的积分内积：

[
\int x(q)y(q)dq
]

## 8.3 Atom→global restriction

体积平均算子：

[
\boxed{
R_g=
(P_g^\top M_HP_g)^{-1}
P_g^\top M_H
}
]

因为：

[
P_g^\top M_HP_g
===============

\operatorname{diag}
(|G_1|,\ldots,|G_{N_g}|)
]

所以：

[
(R_gx_H)_i
==========

\frac1{|G_i|}
\sum_{A_r\subset G_i}
|A_r|x_{H,r}
]

并满足：

[
\boxed{
R_gP_g=I
}
]

## 8.4 低频投影

定义：

[
\boxed{
\Pi_g=P_gR_g
}
]

它满足：

[
\Pi_g^2=\Pi_g
]

证明：

[
\Pi_g^2
=======

# P_gR_gP_gR_g

# P_g(R_gP_g)R_g

# P_gR_g

\Pi_g
]

所以它确实是投影。

低频为：

[
\boxed{
x^{low}=\Pi_gx
}
]

即每个 global cell 内替换成体积平均值。

## 8.5 高频投影

定义：

[
\boxed{
Q_g=I-\Pi_g
}
]

高频为：

[
\boxed{
x^{high}=Q_gx
}
]

它满足：

[
R_gx^{high}=0
]

即每个 global cell 内，高频的体积平均严格为零。

## 8.6 为什么这可以叫高频

这里的“频率”不是 Fourier 频率，而是相对于 global 表示空间的可表达性：

* (\operatorname{Range}(P_g))：global 分辨率可表达的 coarse 子空间；
* (\operatorname{Null}(R_g))：global restriction 看不见的 detail 子空间。

并且在质量内积下：

[
\langle x^{low},x^{high}\rangle_{M_H}=0
]

因此有能量正交分解：

[
|x|_{M_H}^2
===========

|x^{low}|*{M_H}^2
+
|x^{high}|*{M_H}^2
]

在规则等体积 (2^3) 细分网格上，这相当于三维 Haar 分解的：

* 一个 cell-average coarse mode；
* 七个 zero-mean detail modes。

这里是它在不规则透视变换空间中的有限体积推广。

---

# 9. 改进八：只保留 local 相对 global 的高频 residual

设 local endpoint 提升到公共空间后为：

[
y_l
]

Global endpoint 广播到公共空间为：

[
P_gy_g
]

定义 local residual：

[
d=y_l-P_gy_g
]

直接使用 (d) 会同时改变 global 低频和高频。

正确做法是：

[
\boxed{
d^{high}=Q_gd
}
]

最终统一 endpoint：

[
\boxed{
y_H^{sync}
==========

P_gy_g+Q_gd
}
]

由于：

[
Q_gP_g=0
]

也可以写成：

[
y_H^{sync}
==========

P_gy_g+Q_gy_l
]

并严格满足：

[
\boxed{
R_gy_H^{sync}=y_g
}
]

## 9.1 优化意义

它不是经验公式，而是一个带约束的最小二乘解。

给定 local proposal (y_l)，希望寻找离它最近的统一状态 (x)，但要求降回 global 后必须等于 global endpoint：

[
\min_x
\frac12
|x-y_l|_{M_H}^2
]

约束：

[
R_gx=y_g
]

该问题的正交投影解正是：

[
\boxed{
x^*=P_gy_g+Q_gy_l
}
]

所以这个方案的含义是：

> 在所有严格保持 global low-frequency 的高分辨率状态中，选择距离 local proposal 最近的一个。

这就是 global master + local detail 的严格数学依据。

---

# 10. 改进九：部分覆盖时使用 coverage-aware high-pass

## 10.1 原问题

一个 tile 通常只覆盖 global cell 的一部分。

如果直接：

[
h=Q_gd
]

但未覆盖区域被当作零，计算出的 cell 平均会受到 coverage 形状影响。

## 10.2 Coverage 矩阵

定义对角 coverage/confidence 矩阵：

[
C=\operatorname{diag}(c_1,\ldots,c_{N_H})
]

其中：

[
c_r\in[0,1]
]

表示该原子被 local 有效覆盖的程度。

定义 coverage-aware restriction：

[
\boxed{
R_C=
(P_g^\top M_HCP_g)^{-1}
P_g^\top M_HC
}
]

它计算的是每个 global cell 内**已覆盖部分**的体积平均。

Coverage-aware detail：

[
\boxed{
h=C(I-P_gR_C)d
}
]

展开到一个 global cell (G_i)：

[
\mu_i^{cov}
===========

\frac{
\sum_{r\subset G_i}
|A_r|c_rd_r
}{
\sum_{r\subset G_i}
|A_r|c_r
}
]

[
h_r=c_r(d_r-\mu_i^{cov})
]

于是：

[
P_g^\top M_Hh=0
]

也就是：

[
\sum_{r\subset G_i}|A_r|h_r=0
]

即使 local 只覆盖 global cell 的 5%，它也只能在这 5% 内产生正负抵消的 detail，不能整体推高该 global token。

---

# 11. 改进十：多个 tile 先在同一物理原子上融合

## 11.1 原问题

stride 512 后，同一区域通常被多个 tile 覆盖。

如果先分别压回 global token再平均，会丢失：

* local 空间位置；
* 高频分布；
* 遮挡关系；
* tile 之间真正的冲突位置。

## 11.2 正确顺序

对于原子 (A_r)，收集所有覆盖它的 tile residual：

[
d_{r,1},\ldots,d_{r,K_r}
]

先求唯一物理 residual：

[
\bar d_r
========

\operatorname{Merge}
{d_{r,k}}
]

再做：

[
h=Q_g\bar d
]

顺序不能反过来，因为不同 tile 的 coverage 不同，单独高通后再合并不一定仍满足统一的 global-cell 零均值约束。

---

# 12. 改进十一：使用鲁棒融合，而不是普通均值

## 12.1 普通均值的假设

加权均值：

[
\bar d
======

\frac{\sum_kw_kd_k}{\sum_kw_k}
]

是下面问题的解：

[
\min_z
\sum_kw_k|d_k-z|_2^2
]

它隐含假设每个 tile 的误差近似 Gaussian，并且没有大离群值。

但实际 local tile 会有：

* 错误 topology；
* 错误遮挡表面；
* tile 边缘 condition 不完整；
* 不同生成模态。

这些误差更接近重尾或离群分布。

## 12.2 鲁棒 M-estimator

更合理的是：

[
\boxed{
\bar d_r
========

\arg\min_z
\sum_k
w_{r,k}
\rho\left(
|d_{r,k}-z|_{\Sigma^{-1}}
\right)
}
]

其中 (\rho) 可以是：

* Huber loss；
* (L_1)；
* geometric median；
* trimmed quadratic loss。

Huber loss 在小误差时是二次项，保留精度；在大误差时近似线性，限制异常 tile 的影响。

## 12.3 Tile window 的数学依据

设 tile 原始窗口为：

[
w_k(q)\ge0
]

归一化为：

[
\widetilde w_k(q)
=================

\frac{w_k(q)}
{\sum_{k'}w_{k'}(q)}
]

于是：

[
\sum_k\widetilde w_k(q)=1
]

这叫 partition of unity。

它保证如果所有 tile 给出相同常量 field：

[
d_k(q)=d_0
]

融合后仍是：

[
\sum_k\widetilde w_k(q)d_0=d_0
]

因此不会因为 overlap 数量变化产生亮度或 latent 幅值变化。

Raised-cosine window 在边界处值和导数都较平滑，因此比 hard crop 更不容易产生 seam。

## 12.4 Visibility 不是普通置信度

两个 tile 可能看到同一像素方向上的不同表面。

所以必须先用深度或当前几何判断：

[
w_{visibility}\in{0,1}
]

只让真正观察到原子表面的 tile参与融合。

否则会把前表面和后表面的材质、几何 residual 平均在一起。

---

# 13. 改进十二：Global 保持单向 master trajectory

## 13.1 为什么不反向修改 global

若 local 同时修改 global：

[
x_g^{next}
==========

x_g+\Delta t
\left(
v_g+\lambda Rv_l
\right)
]

那么 49 个 local branch 的误差会反馈进唯一 global 主轨迹。

Global 一旦偏离，其误差又会重新广播给所有 tile，形成反馈放大。

## 13.2 数学解释

本方案将 global endpoint 作为硬约束：

[
R_gy_H=y_g
]

Local 只在这个仿射空间中选择高频：

[
\mathcal C(y_g)
===============

{y_H:R_gy_H=y_g}
]

这使 global baseline 始终是可恢复的：

[
R_gy_H^{sync}=y_g
]

因此 global 是稳定锚点，local 是受约束的 detail estimator。

将来若确认 local 很稳定，可以改成软约束：

[
\min_y
\sum_k
|W_k^{1/2}(y-y_k)|^2
+
\lambda_g
|R_gy-y_g|^2
]

但第一版不应这么做，因为它会失去低频严格保持保证。

---

# 14. 改进十三：按阶段分别建立投影，不能只在 C64 分一次高低频

Pixal3D 的不同阶段：

```text
C16 → C32 → Shape512 → C64 → Shape1024 → Texture1024
```

latent 语义、support 和空间尺度都不同。

对于每一阶段 (R)，都应有独立的：

[
P_g^{(R)},\quad
R_g^{(R)},\quad
Q_g^{(R)}
]

例如 Shape1024 local residual 必须相对于 global Shape1024 去低频：

[
d_{1024}^{high}
===============

Q_g^{(1024)}d_{1024}
]

不能只相对于 global C64 去低频。

否则 Shape1024 中 global 已经具有的中频结构，也可能被 local 覆盖。

数学上这是多层嵌套近似空间：

[
V_g^{coarse}
\subset
V_g^{fine}
\subset
V_H
]

每一层都有自己的投影：

[
\Pi_R=P_RR_R
]

因此 detail 应与当前层对应，而不是固定使用一个 projector。

---

# 15. 改进十四：4096 实际是 XY 超分，不是自动的 XYZ 全四倍

由 exact transform：

[
z_g=z_l
]

而：

[
x_g\approx\frac14x_l+b_x(z)
]

[
y_g\approx\frac14y_l+b_y(z)
]

Local C64 cell 的 global-space 尺寸约为：

[
h_x^g\approx\frac14h_{C64}
]

[
h_y^g\approx\frac14h_{C64}
]

[
h_z^g=h_{C64}
]

因此它的等效分辨率约为：

[
256_x\times256_y\times64_z
]

而不是：

[
256^3
]

同理 local Shape1024 提供的直接空间证据约为：

[
4096_x\times4096_y\times1024_z
]

数学依据就是 Jacobian 的三个主尺度：

[
\sigma_x\approx\frac14,\qquad
\sigma_y\approx\frac14,\qquad
\sigma_z=1
]

所以如果最终使用逻辑 C4096³：

* XY 可以写入真实 local 高频；
* Z 方向只能低通复制或由模型先验补全；
* 不能把重复到四层 z-cell 的值称为真实四倍 z 超分。

---

# 16. 改进十五：优先在 latent/endpoint 空间统一，再解码

## 16.1 为什么不直接平均 mesh 或 O-Voxel

Decoder 通常是非线性的：

[
D\left(
\frac{x_1+x_2}{2}
\right)
\neq
\frac{D(x_1)+D(x_2)}2
]

若每个 tile 先独立 decode，再平均输出：

* topology 可能不同；
* surface crossing 可能不同；
* material key 不同；
* 非线性误差已经形成。

所以优先顺序是：

[
\text{统一 latent}
\rightarrow
\text{单次 decoder}
]

## 16.2 为什么 endpoint sync 有助于 latent 可比性

独立 local latent 可能处在不同生成模态。

通过每一步把它们的 clean endpoint 投影到同一个 affine constraint：

[
R_gy_H=y_g
]

local trajectories 被持续约束到同一个 global low-frequency basin。

因此最终 latent 比“独立跑完以后再拼”更有可比性。

## 16.3 如果只能解码后融合

应融合有明确物理意义的 field：

* occupancy probability；
* SDF；
* density；
* surface normal；
* PBR channel。

不能直接 union 49 个 mesh，因为 mesh union 不保证流形性，也不能解决重复内表面。

---

# 17. 改进十六：材质融合使用 global base + local residual

## 17.1 原问题

不同 tile 的绝对 base color 往往存在整体偏色：

[
m_k=m^*+b_k+\delta_k
]

其中：

* (m^*)：真实材质；
* (b_k)：tile-specific global bias；
* (\delta_k)：局部细节。

直接平均绝对材质会把 (b_k) 混入结果。

## 17.2 Residual 形式

使用 global material (m_g) 作为 base：

[
d_k=m_k-m_g
]

只融合 residual：

[
m_H
===

P_gm_g+Q_g\operatorname{Merge}(d_k)
]

如果 tile 偏差主要是低频：

[
b_k\approx\text{constant in global cell}
]

那么 (Q_g) 会去除大部分偏差，只留下局部纹理变化。

这解释了为什么之前固定 global support、只加入小幅 base-color residual 会比直接替换绝对材质稳定。

## 17.3 各材质通道不能使用同一欧氏平均

Base color 可以在线性 RGB 中处理。

Roughness 和 metallic 属于：

[
[0,1]
]

更适合先转 logit：

[
z=\log\frac{x}{1-x}
]

在无界空间融合后再 sigmoid。

法线位于单位球面：

[
n\in S^2
]

应先变换到 global frame，再归一化加权：

[
n=
\frac{\sum_kw_kn_k}
{\left|\sum_kw_kn_k\right|}
]

不能逐通道平均后不归一化。

---

# 18. 改进十七：精确方案与快速三线性近似的关系

公共原子精确求交最严格，但实现成本高。

快速方案可以用局部 cell 内数值积分近似。

对 local cell 取 Gauss 点：

[
q_s,\qquad s=1,\ldots,K
]

权重：

[
\omega_s
]

经过 exact transform：

[
x_s=T_k(q_s)
]

局部体积元：

[
dV_s
====

\omega_s
\left|\det J_{T_k}(q_s)\right|
]

若使用 global cell indicator：

[
\mathbf1_{G_i}(x_s)
]

则交叠体积近似为：

[
\boxed{
\Omega_{kji}
\approx
\sum_s
dV_s\mathbf1_{G_i}(x_s)
}
]

如果为了平滑和速度使用三线性 hat basis (\phi_i(x))，则：

[
\boxed{
\widetilde\Omega_{kji}
======================

\sum_s
dV_s\phi_i(x_s)
}
]

三线性基满足：

[
\sum_i\phi_i(x)=1
]

因此：

[
\sum_i\widetilde\Omega_{kji}
\approx V_{k,j}^l
]

所以三线性权重的正确解释不是“距离决定噪声”，而是：

> 它是对精确空间积分中 indicator function 的平滑、质量集中的有限元近似。

得到 (\widetilde\Omega) 后：

噪声权重：

[
C^{noise}_{kji}
===============

\frac{\widetilde\Omega_{kji}}
{\sqrt{V_{k,j}^lV_i^g}}
]

普通 field 权重：

[
P_{kji}
=======

\frac{\widetilde\Omega_{kji}}
{V_{k,j}^l}
]

因此 exact overlap 和三线性近似使用的是同一理论，只是积分精度不同。

---

# 19. 整个方案可以写成一个约束优化问题

假设第 (k) 个 local tile 在公共空间给出 clean endpoint proposal：

[
y_k
]

希望寻找统一高分辨率 endpoint (y)。

基本问题是：

[
\boxed{
\min_y
\sum_k
\left|
W_k^{1/2}(y-y_k)
\right|_{M_H}^2
}
]

约束：

[
\boxed{
R_gy=y_g
}
]

其中：

* (W_k)：tile coverage、window、visibility；
* (M_H)：原子体积；
* (y_g)：global clean endpoint。

若误差采用 Gaussian 模型，目标是加权二次损失。

若存在异常 tile，将平方损失替换成 Huber/几何中位数损失。

这个优化问题完整表达了方案的目的：

1. 尽量符合所有 local 4096 图像证据；
2. 对不可靠 tile 保持鲁棒；
3. 降到 global 分辨率后必须严格等于 global baseline。

在完整覆盖、二次损失条件下，其结构化解就是：

[
\boxed{
y^*
===

P_gy_g
+
Q_g\operatorname{WeightedMerge}(y_k)
}
]

这不是经验混合公式，而是一个带 coarse constraint 的最小能量解。

---

# 20. 每个改进的保证与局限

| 改进                       | 数学依据                            | 严格保证                      | 不解决什么                  |
| ------------------------ | ------------------------------- | ------------------------- | ---------------------- |
| Exact 相机变换               | 透视投影/反投影                        | global/local 空间往返正确       | 不保证生成内容一致              |
| Token cell/footprint     | 有限体积表示                          | 尺度和部分交叠可定义                | 求交计算成本                 |
| Shared noise             | Gaussian white noise            | 正确边缘分布和交叉协方差              | 不保证 flow 后续不分叉         |
| Anchor/candidate         | 离散 topology + 连续 latent 分离      | global 主体不丢失              | candidate 是否真实仍需判断     |
| Clean endpoint sync      | 概率路径线性反解                        | 同步同一个目标变量                 | 依赖 path 公式正确           |
| (PR/Q) 投影                | 质量正交投影                          | global low frequency 严格保持 | 高频本身可能仍是错误预测           |
| Coverage-aware high-pass | masked finite-volume projection | 部分覆盖不改变 global 平均         | 不解决遮挡错误                |
| Robust merge             | M-estimation                    | 限制异常 tile 影响              | 不能恢复所有 tile 都错的情况      |
| Visibility gating        | 几何可见性                           | 不混合不同表面                   | 依赖当前深度质量               |
| 分层 projector             | 多尺度近似空间                         | 每层只补当前层 detail            | 实现复杂度较高                |
| XY/Z 各向异性                | exact Jacobian                  | 不伪称 z 方向四倍信息              | z 高频仍有限                |
| 统一 latent 后解码            | 非线性算子性质                         | 减少解码后拓扑冲突                 | 要求 latent mapping 足够一致 |
| 材质 residual              | base + detail 分解                | 减少 tile 低频偏色              | local 高频仍可能错误          |

---

# 21. 最核心的三个数学支柱

整个 4096 超分方案最终依赖三条原则。

## 空间

[
\boxed{
\widetilde L_{k,j}=T_k(L_{k,j})
}
]

所有关系由 exact mapped footprint 决定，不由点距离决定。

## 噪声

[
\boxed{
\epsilon(D)
===========

\frac1{\sqrt{|D|}}\int_DdW
}
]

保证同一物理空间共享同一个随机 realization，同时每个分支保持标准高斯。

## Flow

[
\boxed{
y_H^{sync}
==========

P_gy_g
+
Q_g\operatorname{RobustMerge}(y_l)
}
]

其中：

[
Q_g=I-P_gR_g
]

保证 local 只补充 global 当前层无法表示的 detail，并严格满足：

[
\boxed{
R_gy_H^{sync}=y_g
}
]

因此这个方案不是“给 global 加一点 local”，而是：

> 在保持 global coarse prediction 不变的约束下，求最符合多个 local 4096 图像观测的高分辨率 latent 状态。
