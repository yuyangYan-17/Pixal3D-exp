理解后续关于 Pixal3D 的讨论时，统一以官方源码的相机与投影流程为准：Pixal3D 先对输入图像做前景分割、裁剪和 padding 得到实际用于生成的约束图，然后 MoGe-2 从这张预处理后的图像估计归一化相机内参，代码由 `fx` 计算水平视场角 `camera_angle_x = 2*atan(W/(2fx))`；FOV 得到以后，Pixal3D 并不是让 camera distance 独立估计，而是结合当前 `mesh_scale` 和约束图边界反算 `distance`。具体地，`distance_from_fov()` 取生成网格边界点 `grid_point=(-1,0,0)`，先经过官方固定轴变换，再做 `p = p / mesh_scale / 2`，因此该点在投影所使用的连续三维空间中位于 `x=-1/(2*mesh_scale)`；然后根据 FOV 得到像素焦距 `f = W/(2*tan(FOV/2))`，调整相机沿光轴的位置，使这个生成空间边界点恰好投影到约束图指定的边界像素 `target_point`，从而得到 `distance`。所以 Pixal3D 中的 FOV、distance、mesh_scale 和约束图覆盖范围是一套联合定义的相机—生成空间合同：`mesh_scale` 决定离散生成网格映射到投影空间后的实际尺度，FOV 决定射线张角，distance 再根据“生成空间边界应落在约束图什么位置”反算出来；例如默认 `extend_pixel=0` 时，可理解为相机距离被选择到刚好让当前 `mesh_scale` 下的生成空间横向范围铺到图像边界。后续 SS、Shape512、Shape1024、Texture1024 虽然分别使用 C16、C32、C64 等不同 sparse grid resolution，但用于图像条件时都遵循同一套规则：先在每个轴上构造 `linspace(-1,1,R)` 的三维查询网格，经过固定坐标旋转，再除以 `2*mesh_scale` 得到实际参与透视投影的三维位置；相机 transform 中沿光轴的平移量设为前面求得的 `distance`，再用 `camera_angle_x` 得到焦距，将每个三维 query 投影到约束图/DINO/NAF 特征图上取得对应的图像 condition。本质上是在规定某一组 sparse-grid 三维位置经过 `mesh_scale` 转成投影空间后，应通过什么 FOV 和 camera distance 映射到约束图的哪些像素；如果局部切块后改变了约束图窗口、生成空间范围或 `mesh_scale`，相应的 FOV、distance、三维 query 坐标和图像投影关系也必须同步重新计算，而不能只对 C32/C64 整数坐标做平移或 reindex。
